<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <defs>
    <style>
      .primary { fill: #ff4500; }
      .cut { fill: #ffffff; }
    </style>
  </defs>

  <!-- Base Circle -->
  <circle cx="50" cy="50" r="48" class="primary"/>

  <!-- C Cutout -->
  <circle cx="50" cy="50" r="30" class="cut"/>
  <rect x="50" y="15" width="40" height="70" class="primary"/>
</svg>